# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: nf-core-bacass-latest
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - fastqc=0.11.8
  #- awscli=1.15.45 not needed if we use the right base image!?
  - multiqc=1.6
  - unicycler=0.4.6
  - skewer=0.2.2
  - kraken2=2.0.7_beta
  - quast=5.0.2
  - prokka=1.13
  - bandage=0.8.1
  # - r-markdown for markdown_to_html.r
